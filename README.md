# lead_barry

The lead-acid battery management controller written in Rust for Raspberry Pi Pico.

### Some build command

```bash
# Build and run main firmware package
cargo run --vv --package lead_barry --target thumbv6m-none-eabi --features defmt
```
